export const environment = {
  // Relative — in production the frontend and backend are served from the
  // same origin, with /api/* routed to the backend service (see vercel.json).
  apiBase: '/api'
};
